checkout:
  post:
    - echo "CIRCLE_GIT_TAG=$(git tag --points-at $CIRCLE_SHA1)" >> ~/.circlerc
    - echo $CIRCLE_GIT_TAG
test:
  override:
    - lsb_release -a: { parallel: true }
    - pwd: { parallel: true }
    - ls: { parallel: true }
