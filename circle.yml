test:
  override:
    - lsb_release -a: { parallel: true }
    - pwd: { parallel: true }
    - ls: { parallel: true }
    - sleep 120
    - echo 'awesomest sauce' > $CIRCLE_ARTIFACTS/awesome.txt
teardown:
  post:
    - echo gordon sez this is good

notify:
  webhooks:
    # A list of hook hashes, containing the URL field
    - url: https://requestb.in/1hj2xns1
